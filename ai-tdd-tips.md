---
description: AIに実装を任せるときに、指示の曖昧さで失敗しないための開発手法メモ。
---

# AI駆動開発Tips（テストケース駆動）

## 課題

AIに「機能を実装して」と直接指示すると、指示文が曖昧な場合にAIの解釈が要件とズレて、意図しない実装になることがある。

## 手法：テストケース → テストコード → 実装 の順で進める

1. **テストケースの作成（AIと壁打ち）**\
   実装前に、どんな入出力・異常系・境界値があるかをAIと対話しながら洗い出し、テストケース一覧を作る。
2. **テストコードの作成**\
   洗い出したテストケースを元に、AIにテストコードを書かせる。
3. **実装**\
   そのテストコードを満たすように、AIに機能を実装させる。

自然言語の曖昧な指示を、実行可能な仕様（テスト）に変換してから実装に入るのがポイント。TDD（テスト駆動開発）をAI活用の文脈に応用したイメージ。

## 注意点（人間の責務）

この手法の効果は、**テストケースの網羅性**にほぼ依存する。テストケースに漏れがあると、AIはそのテストにだけ最適化した実装を作ってしまい、「テストは通るが要件は満たしていない」状態になり得る。そのため以下は人間が意識的にチェックする。

* 壁打ちの段階で、異常系・境界値・認可周りなど抜けがちなケースを意識的に問い詰める
* AIが生成したテストコード自体（アサーションの妥当性など）もレビューする
